// Consumed by `resource`, `jobs`, and `ui` in later tasks; not yet wired into `main`.
#[allow(dead_code)]
mod config;

#[allow(dead_code)]
mod model;

#[allow(dead_code)]
mod stats_math;

fn main() {
    println!("pitwall");
}
